version: '3.8'

services:
  db:
    image: mongo:6.0
    container_name: devportfolio-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db

volumes:
  mongodata:
